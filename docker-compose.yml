services:
  crate:
    build: .
    container_name: crate
    restart: unless-stopped
    ports:
      - "4533:4533"
    volumes:
      - crate-data:/data
      - /path/to/music:/music:ro
    environment:
      - TZ=UTC

volumes:
  crate-data:
